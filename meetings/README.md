# Meeting Notes

Here are the meeting notes for our meetings stored in markdown.

## Adding a new note

Looking to add a new note? Please run the python script called [new.py](./new.py) using either `python new.py` or `python3 new.py` to create it automatically!
